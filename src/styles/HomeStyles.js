import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    title: {
        color: '#000',
        fontWeight: 'bold',
        marginHorizontal: 10,
        marginVertical: 10,
    },

    btnDiscovery: {
        marginVertical: 10,
        marginLeft: 10,
        padding: 8,
        width: 120,
        textAlign: 'center',
        color: '#FF9900',
        backgroundColor: '#EEE',
        borderRadius: 20,
    }
})

export default styles;
