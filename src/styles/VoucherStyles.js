import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    titleContent: {
        marginHorizontal: 16,
        marginVertical: 8,
        marginTop: 20,
        color: '#000',
        fontSize: 18,
        fontWeight: 'bold',
    },
})

export default styles;